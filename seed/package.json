{
  "name": "restaurant-search-seed",
  "version": "0.1.0",
  "description": "Seed scripts for restaurant search demo",
  "type": "module",
  "scripts": {
    "fetch": "node fetchData.js",
    "ingest": "node --env-file=.env ingest.js",
    "create-index": "node --env-file=.env createIndex.js",
    "seed": "npm run fetch && npm run ingest && npm run create-index"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongodb": "^6.6.2"
  }
}

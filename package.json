{
  "name": "restaurant-search-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix seed",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run frontend --prefix frontend",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend",
    "seed": "npm run seed --prefix seed",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  }
}
